# Task index

Every command this repo publishes, with what it does. `inv --list` prints the same thing on a
machine that already has PULSE installed; this page is for the rest of the time — deciding whether
something exists before you clone, or finding the name of the task you half-remember.

The table is generated from the invoke namespace by `inv catalog.render-tasks` and guarded by a unit
test, so a task cannot be added, renamed or re-described without this page following. Each row's
text is the first sentence of the task's own docstring; `inv --help <task>` prints the whole thing,
including its options.

**How to read the names.** `inv <namespace>.<task>` reads as an imperative: the namespace is the
subject, the task is the action — `inv apt.install-base` is "apt: install base". A few namespaces
are themselves the action (`setup`, `verify`, `clean`, `deploy`, `test`), so their leaves name a
scope instead: `verify.all`, `test.unit`. Names like `status`, `check`, `list` and `diff` never
modify anything.

Not every task is meant for everyday use. The ones a normal machine actually needs are in
[index.md](index.md)'s quick start and maintenance sections; the rest exist for a specific situation
— a corporate proxy, a WSL distro, a container build, a docs edit.

!!! warning

    Tasks that write to a live GNOME session (`gnome.configure`, `gnome.install-extensions`,
    `screenshot.enable`/`disable`) need a real Wayland/X11 session and change your desktop
    immediately. Their read-only siblings (`gnome.status`, `screenshot.status`) are safe anywhere,
    and `PULSE_DRY_RUN=1` turns any task into a report of what it would do.

<!-- PULSE::task-index -->

| Task                                    | What it does                                                                                                                                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `inv agents.wire-claude-hook`           | Wire Claude Code's Bash tool to auto-activate this project's direnv environment on every call, by writing <dir>/.claude/settings.json with env.CLAUDE_ENV_FILE and a PreToolUse/Bash hook that…        |
| `inv ai.check-rule-prerequisites`       | Report ~/AGENTS.md rules whose declared prerequisite is no longer installed.                                                                                                                           |
| `inv ai.install-skills`                 | Ensure .agents/skills exists with .claude/skills symlinked to it, then install every skill declared via a `skills` field anywhere in setup.toml                                                        |
| `inv allowlist.apply`                   | Merge the reviewed Bash allow/ask rules into ~/.claude/settings.json's `permissions` block — the only thing this task ever touches there.                                                              |
| `inv allowlist.check-coverage`          | Does every child of a node-with-children actually have its own renderable rule?                                                                                                                        |
| `inv allowlist.check-man-deps`          | Does any registered tool's --help invocation secretly depend on a separately-installed man-page-rendering package?                                                                                     |
| `inv allowlist.classify`                | Classify each tool's extracted nodes (subcommands, and nested subcommands for any tool with max_depth > 1) as read_only/write/dangerous via a headless `claude -p` call, isolated in a scratch cwd…    |
| `inv allowlist.extract`                 | Capture --help text per registered tool (tools.toml), recursing into the subcommand tree for any tool with max_depth > 1.                                                                              |
| `inv allowlist.reconfirm`               | Second-pass LLM reclassification for everything currently sitting at `needs_review` (subcommands and flags alike)                                                                                      |
| `inv allowlist.render`                  | Print the reviewed subset of rules as Claude Bash(...) allow/ask rules or Copilot chat.tools.terminal.autoApprove regex rules.                                                                         |
| `inv allowlist.review`                  | Show tools with unreviewed classifications (new or changed since the last reviewed snapshot) and, on confirmation, mark them reviewed.                                                                 |
| `inv allowlist.status`                  | Quick table: which registered tools are installed, stale (version changed since last classify), or still unreviewed.                                                                                   |
| `inv apt.audit-keys`                    | Audit apt key hygiene — report legacy keys, old-style trust, stale backups.                                                                                                                            |
| `inv apt.clean-cache`                   | Remove apt's downloaded .deb cache for packages no longer available at their cached version (`apt-get autoclean`) — conservative, keeps .debs for currently-installed versions cached for reinstall.   |
| `inv apt.clean-cache-full`              | Remove apt's entire downloaded .deb cache (/var/cache/apt/archives), including .debs for currently-installed packages — apt just re-downloads them if reinstalled.                                     |
| `inv apt.configure`                     | Write /etc/apt/apt.conf.d/99-pulse: disable dpkg progress bars.                                                                                                                                        |
| `inv apt.install-base`                  | Install base apt packages from config.                                                                                                                                                                 |
| `inv apt.install-debs`                  | Install packages sourced from GitHub releases or direct deb URLs.                                                                                                                                      |
| `inv apt.install-repos`                 | Set up external apt repos and install their packages.                                                                                                                                                  |
| `inv apt.refresh-keys`                  | Re-download GPG keys for all enabled apt-repo sources.                                                                                                                                                 |
| `inv apt.uninstall`                     | Purge the apt packages declared for a setup.toml section (any method), e.g. inv apt.uninstall citrix-workspace.                                                                                        |
| `inv apt.upgrade-debs`                  | Upgrade all deb-github packages to their latest versions (re-downloads and reinstalls each).                                                                                                           |
| `inv catalog.render-packages`           | Regenerate docs/packages.md's catalog table from setup.toml.                                                                                                                                           |
| `inv catalog.render-tasks`              | Regenerate docs/tasks.md's task index from the invoke namespace.                                                                                                                                       |
| `inv certs.check`                       | Read-only diagnostic: bundle install status, ~/.zshenv env vars, Java cacerts.                                                                                                                         |
| `inv certs.install`                     | Install the corporate CA bundle into the OS trust store and export it for python/node/awscli. --bundle=path overrides the \[certs\] table in ~/.config/power-user-linux-setup/identity.toml.           |
| `inv chrome.fix-launchers`              | Label every Chrome PWA launcher by profile and unhide the primary profile's.                                                                                                                           |
| `inv chrome.status`                     | Report who starts Chrome at login, then every PWA launcher's profile, visibility and flag.                                                                                                             |
| `inv ci.check-actions`                  | Report which GitHub Actions used in this repo's workflows are behind their latest release.                                                                                                             |
| `inv ci.status`                         | Report recent GitHub Actions runs for a branch, and stop if the latest one failed.                                                                                                                     |
| `inv clean.all`                         | Everything reclaimable, conservatively: caches() plus Docker's conservative prune (stopped containers, dangling images — see `docker.clean`).                                                          |
| `inv clean.all-full`                    | Everything reclaimable, fully wiped: caches_full() plus Docker's full prune (also removes unused-but-tagged images — see `docker.clean-full`).                                                         |
| `inv clean.caches`                      | Conservatively remove build/download caches left behind by apt, uv, npm, and cargo installs (each self-skips if that tool isn't installed)                                                             |
| `inv clean.caches-full`                 | Fully wipe apt/uv/npm/cargo caches (each self-skips if that tool isn't installed)                                                                                                                      |
| `inv configs.diff`                      | Show what `configs.pull` would change, plus any repo-tasks-quality entry this project's dependency-groups.dev has fallen behind on, without writing anything.                                          |
| `inv configs.ensure-deps`               | Ensure this project's pyproject.toml declares every quality-tooling dependency repo-tasks needs (sourced from repo-tasks' own dependency-groups.quality                                                |
| `inv configs.pull`                      | Materialize ruff.toml/pyrightconfig.json/dprint.json/pytest.ini/zizmor.yml/.editorconfig from the canonical source into this repo's root.                                                              |
| `inv deploy.all`                        | Deploy every path this repo declares under ~ — or one package's with --name — never destroying content PULSE can't prove it wrote.                                                                     |
| `inv deploy.status`                     | Report every path this repo deploys under ~, and whether it still matches its repo source.                                                                                                             |
| `inv deps.audit`                        | Check the locked dependency set for known advisories (uv audit --locked).                                                                                                                              |
| `inv deps.check`                        | Check uv.lock is up-to-date with pyproject.toml, read-only, no .venv needed.                                                                                                                           |
| `inv deps.export`                       | Export uv.lock to a pinned requirements.txt (--locked, non-editable) for non-uv-aware consumers — SBOM/vulnerability scanners, plain-pip CI steps.                                                     |
| `inv deps.list`                         | List what's actually installed in .venv (uv pip list).                                                                                                                                                 |
| `inv deps.lock`                         | Write/update uv.lock.                                                                                                                                                                                  |
| `inv deps.tree`                         | Show the full resolved dependency tree from uv.lock (uv tree).                                                                                                                                         |
| `inv dev-env.setup`                     | Run once after cloning: create/refresh .venv, let direnv auto-activate it, and wire Claude Code's Bash tool to auto-activate it too.                                                                   |
| `inv devcontainer.check`                | Read-only dry run for the container distribution path: reports what `inv setup`'s packages+shell phases would do under the recommended container tag profile, without running anything for real        |
| `inv devcontainer.print-exclude-tags`   | Print CONTAINER_EXCLUDE_TAGS, comma-separated, no surrounding text.                                                                                                                                    |
| `inv devcontainer.print-mounts`         | Host-side interactive helper: discover credential-shaped directories/sockets on this machine (~/.ssh or $SSH_AUTH_SOCK, ~/.config/power-user-linux-setup, the corporate CA bundle from identity.toml,… |
| `inv devcontainer.render-docs`          | Regenerate the tag-table + example block in docs/dev-container.md from CONTAINER_EXCLUDE_TAGS/_TAG_DESCRIPTIONS (HTML-comment-marked                                                                   |
| `inv docker.clean`                      | Prune stopped containers, dangling images, and unused networks/build cache (`docker system prune -f`).                                                                                                 |
| `inv docker.clean-full`                 | Prune everything `docker.clean` does, plus all images not currently used by a container — tagged or not (`docker system prune -af`).                                                                   |
| `inv docker.configure`                  | Merge log limits and DNS into /etc/docker/daemon.json, add user to docker group.                                                                                                                       |
| `inv docker.configure-credential-store` | Point docker (and, through oras, helm) at the OS secret store instead of a plaintext file.                                                                                                             |
| `inv docs.build`                        | Build the docs site with zensical in strict mode (fails on any warning).                                                                                                                               |
| `inv docs.clean`                        | Remove the built docs site (site/).                                                                                                                                                                    |
| `inv docs.link-check`                   | Check that every relative link in the repo's markdown resolves to a file in this repo.                                                                                                                 |
| `inv docs.serve`                        | Serve the docs site locally with live reload (zensical serve).                                                                                                                                         |
| `inv fonts.check`                       | Report every place on this machine that names a font, and whether it agrees.                                                                                                                           |
| `inv fonts.configure`                   | Set the configured monospace font as system default, GNOME Terminal, and VS Code font.                                                                                                                 |
| `inv fonts.install`                     | Download and install Nerd Font families to ~/.local/share/fonts.                                                                                                                                       |
| `inv fonts.render-configs`              | Rewrite the repo-side configs that name a font from `\[settings.fonts\]`.                                                                                                                              |
| `inv git.apply-settings`                | Apply global git configuration (idempotent, no identity data required).                                                                                                                                |
| `inv git.configure`                     | Set up per-directory git identities from ~/.config/power-user-linux-setup/identity.toml.                                                                                                               |
| `inv gnome.clean`                       | Remove user extensions not enabled in setup.toml (system/apt extensions are untouched).                                                                                                                |
| `inv gnome.configure`                   | Apply dconf settings for all enabled extensions — re-run without reinstalling.                                                                                                                         |
| `inv gnome.enable`                      | Re-enable installed GNOME extensions from setup.toml without reinstalling missing ones.                                                                                                                |
| `inv gnome.install-extensions`          | Install and enable GNOME Shell extensions declared in setup.toml via gext.                                                                                                                             |
| `inv gnome.status`                      | Show GNOME extension diagnostic: dconf flags, active state, and setup.toml alignment.                                                                                                                  |
| `inv gnome.update`                      | Update all gext-managed GNOME Shell extensions.                                                                                                                                                        |
| `inv home.list-claims`                  | List every path in ~ this repo has a claim on, classified by writer, authority and tier.                                                                                                               |
| `inv ide.configure-pycharm`             | Copy font settings into the active PyCharm config directory.                                                                                                                                           |
| `inv identity.init`                     | Interactive wizard for ~/.config/power-user-linux-setup/identity.toml: pick simple (one name/email, one projects directory                                                                             |
| `inv net.check`                         | Diagnose this machine's network against what a PULSE run actually needs.                                                                                                                               |
| `inv node.clean-cache`                  | Garbage-collect npm's package cache (~/.npm), removing invalid/unneeded entries while verifying the rest — conservative, npm's own recommended way to reclaim cache space.                             |
| `inv node.clean-cache-full`             | Wipe npm's entire package cache (~/.npm).                                                                                                                                                              |
| `inv node.install`                      | Install nvm, Node.js, and global npm packages from config.                                                                                                                                             |
| `inv proxy.check`                       | Diagnose corporate-proxy state: environment, candidate address, auth scheme, Px/daemon status.                                                                                                         |
| `inv proxy.fix`                         | Install px if missing and (re)write its config + daemon, without touching credentials.                                                                                                                 |
| `inv proxy.install`                     | Full flow: detect, capture a credential if the probe requires one, configure + start the daemon, then verify it actually authenticates before pointing every terminal at it.                           |
| `inv python.clean-cache`                | Prune unreachable entries from uv's build/wheel cache (~/.cache/uv) — conservative, keeps entries still reachable from any installed environment.                                                      |
| `inv python.clean-cache-full`           | Wipe uv's entire build/wheel cache (~/.cache/uv).                                                                                                                                                      |
| `inv python.install-tools`              | Install global Python CLI tools via uv tool install.                                                                                                                                                   |
| `inv python.set-default`                | Change settings.uv_python_default in setup.toml and re-point the live python/python3 shims at it (uv python install <version> --default, unless uv_python_set_default is false).                       |
| `inv quality.check`                     | CI-style gate: every check, no changes written.                                                                                                                                                        |
| `inv quality.dockerfile-check`          | Run hadolint against every Dockerfile in the repo.                                                                                                                                                     |
| `inv quality.fix`                       | Fix everything auto-fixable: ruff --fix, ruff format, dprint fmt, shfmt -w.                                                                                                                            |
| `inv quality.format-apply`              | Apply formatting: ruff format, then dprint fmt.                                                                                                                                                        |
| `inv quality.format-check`              | Check formatting (ruff format, dprint) without writing changes.                                                                                                                                        |
| `inv quality.lint-apply`                | Run ruff's linter and apply auto-fixes.                                                                                                                                                                |
| `inv quality.lint-check`                | Run ruff's linter (no fixes).                                                                                                                                                                          |
| `inv quality.precommit`                 | Fix, then every step of check, then build the docs site — the one command to run before considering a change done, with no need to know or invoke the individual tools.                                |
| `inv quality.shell-check`               | Run shellcheck against every *.sh file in the repo.                                                                                                                                                    |
| `inv quality.shell-format-apply`        | Apply shell script formatting (shfmt).                                                                                                                                                                 |
| `inv quality.shell-format-check`        | Check shell script formatting (shfmt) without writing changes.                                                                                                                                         |
| `inv quality.type-check`                | Run basedpyright's type checker.                                                                                                                                                                       |
| `inv quality.verify-types`              | Report basedpyright's type-completeness for each package under src/ — how much of the published API a consumer sees with a known type.                                                                 |
| `inv quality.workflow-check`            | Check every GitHub Actions workflow file (.github/workflows/*.yml): actionlint for correctness, zizmor for security.                                                                                   |
| `inv screenshot.disable`                | Remove the 2 Flameshot keybindings and restore GNOME's shipped screenshot defaults.                                                                                                                    |
| `inv screenshot.enable`                 | Bind PrtSc/Shift+PrtSc to Flameshot (save + clipboard, no dialog), disabling the matching GNOME defaults.                                                                                              |
| `inv screenshot.render-docs`            | Regenerate docs/shortcuts.md's table from this module's own keybinding data.                                                                                                                           |
| `inv screenshot.status`                 | Show current screenshot shortcut state: GNOME defaults vs the Flameshot bindings.                                                                                                                      |
| `inv setup`                             | Run full machine setup, in phases (system, packages, shell, desktop) — delegates to wsl.install under WSL.                                                                                             |
| `inv ssh.add`                           | Add this machine's SSH keys to the agent, skipping any it already holds.                                                                                                                               |
| `inv ssh.check`                         | Diagnose which ssh-agent this shell talks to and whether it holds the declared keys.                                                                                                                   |
| `inv ssh.configure`                     | Write ~/.ssh/config from identity.toml (idempotent PULSE block).                                                                                                                                       |
| `inv ssh.create-keys`                   | Create ed25519 SSH keys for each unique email in identity.toml (skips existing).                                                                                                                       |
| `inv ssh.forward`                       | Copy public keys to remote (non-git) server hosts from identity.toml.                                                                                                                                  |
| `inv system.cap-journal-size`           | Cap persistent journal size (default: 500M) and restart journald if changed.                                                                                                                           |
| `inv system.configure-dns`              | Configure DNS via systemd-resolved drop-in (Cloudflare + Google fallback).                                                                                                                             |
| `inv system.disable-ipv6`               | Ensure IPv6 is disabled in /etc/sysctl.conf and apply immediately.                                                                                                                                     |
| `inv system.install-apparmor-profiles`  | Install AppArmor profiles declared in setup.toml with method = 'apparmor-profile'.                                                                                                                     |
| `inv system.set-initramfs-compression`  | Set initramfs compression algorithm (default: xz) and rebuild if changed.                                                                                                                              |
| `inv system.set-locale`                 | Set the system locale via localectl, preserving every variable not named here.                                                                                                                         |
| `inv system.write-curlrc`               | Write ~/.config/curlrc: silent, show errors, follow redirects.                                                                                                                                         |
| `inv test.all`                          | Run every tier: the unit tests, then the whole integration tier.                                                                                                                                       |
| `inv test.coverage`                     | Report line coverage for the unit tier (pytest --cov, needs pytest-cov from the repo-tasks-quality group).                                                                                             |
| `inv test.integration`                  | Run the whole integration tier against real local services (Docker, a package index).                                                                                                                  |
| `inv test.regression`                   | Run everything in the integration tier that isn't smoke (`-m "not smoke"`) — the broad, slower half.                                                                                                   |
| `inv test.smoke`                        | Run the fast, happy-path slice of the integration tier (`-m smoke`) — enough to know the system is wired up at all.                                                                                    |
| `inv test.unit`                         | Run the unit tier — no Docker, no network, nothing outside tmp_path.                                                                                                                                   |
| `inv test.untested-modules`             | Check every module in a src-layout package has a tests/unit/test_<module>.py.                                                                                                                          |
| `inv test.workflows`                    | Run the repo's GitHub Actions workflows locally with act (nektos/act), in Docker containers standing in for the hosted runners.                                                                        |
| `inv tools.clean-cache`                 | Remove cargo's compressed crate-download cache (~/.local/share/cargo/registry/cache), if rust is installed                                                                                             |
| `inv tools.clean-cache-full`            | Remove cargo's entire registry cache (~/.local/share/cargo/registry) — compressed downloads, extracted sources, and index — if rust is installed.                                                      |
| `inv tools.install`                     | Install tools that use official installer scripts, direct binary downloads, or archives.                                                                                                               |
| `inv verify.all`                        | Prove every package this run installed actually works, not just that it's present.                                                                                                                     |
| `inv wsl.check`                         | Diagnose WSL-specific risks before running PULSE tasks: systemd, DNS, Docker, WSLg.                                                                                                                    |
| `inv wsl.fix`                           | Fix common /etc/wsl.conf misconfigurations: systemd=true, generateResolvConf left at its safe default (true — WSL manages /etc/resolv.conf itself, mirroring whatever DNS server Windows itself uses). |
| `inv wsl.install`                       | Run the docs/wsl.md recommended install sequence in one shot, as named phases: wsl config, system config (locale/DNS, only if systemd/DNS are actually live yet), packages, then shell                 |
| `inv zsh.configure`                     | Add or update the zsh blocks declared in setup.toml, and take back the ones that no longer apply to this machine.                                                                                      |
| `inv zsh.configure-omz`                 | Update Oh My Zsh theme and plugins list in ~/.zshrc in-place.                                                                                                                                          |
| `inv zsh.configure-p10k`                | Seed ~/.p10k.zsh from the repo baseline, through tasks/deploy.py's writer.                                                                                                                             |
| `inv zsh.fix-history`                   | Recover a corrupt ~/.zsh_history using strings(1) to strip non-printable bytes.                                                                                                                        |
| `inv zsh.set-default-shell`             | Set zsh as the login shell (usermod -s, not chsh — chsh's PAM password prompt doesn't work in a non-interactive/piped session the way sudo -A does).                                                   |

<!-- /PULSE::task-index -->

## See also

- [How it works](configuration.md) — what `inv setup` runs, in order
- [Updating and removing](updating.md) — the tasks worth running after setup
- [Package catalog](packages.md) — what the install tasks install
